global:
  scrape_interval: 1m
  scrape_timeout: 10s

scrape_configs:
  - job_name: igmo-app
    metrics_path: /actuator/prometheus
    sample_limit: 10000
    static_configs:
      - targets:
          - 127.0.0.1:8080
        labels:
          service: igmo
          env: prod
